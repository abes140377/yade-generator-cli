name: k8s_create
description: A YADE Infrastructure As Code template
version: 0.1.0+1

environment:
  mason: ">=0.1.0-dev <0.1.0"

vars:
  name:
    type: string
    description: The name of the k8s cluster
    default: example
    prompt: What is the k8s cluster name?
